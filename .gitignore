# venv + outputs (generated)
.venv/
.mcp-venv/
digests/
data/cache/
data/.scweet_cache/

# secrets (must never push)
data/webhook.txt
data/email_config.json
*.env
.env

# python
__pycache__/
*.pyc
*.pyo

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# === Privacy: watchlist data (not for OSS) ===
# author_boost.json is the runtime input; users derive their own.
# The raw scrape is private — never commit.
data/following.json
data/relevant.json
data/rt_authors.json
data/tweets.json
data/FOLLOW_LIST.md
data/author_boost.json
